version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: catherinepope/mac
    steps:
      - run: echo "Hello. I'm (hopefully) on an Mac Runner! Fingers crossed."
