version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: catherinepope/macmini
    steps:
      - run: echo "Hello. I'm on an MacMini Runner!"
